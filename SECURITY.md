# bottari 보안 설계

## 키 계층

- 최초 `init` 에서 무작위 32바이트 **DEK**가 만들어지고, 이후 모든 콘텐츠는
  DEK로 AES-256-GCM 봉인됩니다.
- 사용자 암호는 scrypt(N=2¹⁷, r=8, p=1, salt 16B)로 **KEK**를 만들고, KEK는
  DEK를 래핑해 `bottari.meta.json` 에 둡니다. 암호 변경은 DEK 재래핑 1회로
  끝나며 이미 올린 데이터는 다시 암호화하지 않습니다.
- GCM 태그가 암호 검증을 겸합니다. 틀린 암호는 복호가 아니라 인증 실패입니다.
- 파생키는 HKDF(DEK, 용도 문자열)로 만듭니다. 객체 파일명은
  HMAC(파생키, 평문 SHA-256)이라 Drive에는 "어떤 파일을 갖고 있는지"에 대한
  어떤 힌트도 남지 않습니다.

## 봉투 형식

```
"BTR1" | u32le 헤더 길이 | 헤더 JSON | 암호문 | 16B GCM 태그
```

매직·길이·헤더 전체가 AAD로 들어가므로 암호문 앞의 어떤 바이트도 태그를
깨지 않고는 바꿀 수 없습니다. 헤더의 객체 id 가 봉투에 결속되어 있어, Drive
에서 파일 이름을 바꿔치기해도 열 때 거부됩니다.

클라우드에서 내려온 키 레코드의 scrypt 파라미터는 **키 유도 전에** 상한
(logN 14~20)을 검사합니다 — 적대적 메타 파일이 2³¹을 요구해 메모리를
고갈시키는 경로를 차단합니다.

## 시크릿 취급

- `~/.claude/.credentials.json`, `~/.codex/auth.json` 등 자격증명 파일은
  동기화 대상이 아니며, 이름만으로도 업로드가 거부됩니다.
- 설정 안의 인라인 토큰(Authorization 헤더 등)은 OS 자격증명 저장소
  (Windows DPAPI / macOS Keychain / Linux libsecret)로 옮겨지고 자리에는
  `${BOTTARI_SECRET:이름}` 이 남습니다. 다른 컴퓨터에서는
  `bottari secrets set <이름>` 으로 채웁니다.
- 업로드 직전 모든 자산·설정 버퍼를 자격증명 패턴으로 검사하고, 하나라도
  발견되면 **업로드 전체가 중단**됩니다(fail-closed). 문서 예제 같은 오탐은
  지문 단위로만 허용할 수 있습니다. 세션 기록은 사용자가 붙여넣은 키가 있을
  수 있어 차단 대신 집계·경고합니다(어차피 암호화되어 저장됩니다).
- MCP로 충돌 diff 를 보여줄 때는 모델 컨텍스트로 나가기 전에 자격증명
  패턴을 가립니다.

## 위협 모델의 경계

- **Drive 를 볼 수 있는 자**: 파일 개수·크기·업로드 시각만 얻습니다. 내용,
  파일명, 경로 구조는 얻지 못합니다.
- **암호를 잊은 사용자**: 데이터를 열 방법이 없습니다. 유일한 예외는 열쇠를
  `--remember-key` 로 자격증명 저장소에 보관해 둔 컴퓨터가 남아 있는 경우입니다.
- **GCM nonce**: 객체마다 무작위 96비트입니다. 이 도구의 객체 수 규모
  (수만 개)에서 충돌 확률은 무시 가능하지만, 수십억 객체 규모의 용도로는
  설계되지 않았습니다.
- **머신 위조**: machineId는 신뢰 경계가 아닙니다. Drive 계정에 쓸 수 있는
  자는 이미 모든 것을 할 수 있습니다.

## OAuth 클라이언트

내장된 클라이언트 ID/secret 은 RFC 8252 §8.5 에 따라 기밀이 아닙니다 —
설치형 앱의 secret 은 앱을 식별할 뿐 사용자를 보호하지 않으며, 모든 배포본에
동일하게 들어 있습니다. 요청 범위는 `drive.file`(이 앱이 만든 파일만) 하나
입니다. 포크를 재배포한다면 `BOTTARI_CLIENT_ID`/`BOTTARI_CLIENT_SECRET` 로
자체 클라이언트를 쓰는 것을 권합니다.

## 저장소의 개인정보 규칙

소스·주석·문서·테스트 어디에도 실제 사용자명, 컴퓨터명, 실제 폴더명이
들어가지 않습니다(허용되는 것은 저작권자 표기뿐). `scripts/check-pii.mjs` 가
일반 패턴과 gitignore된 로컬 거부 목록으로 이를 강제하며, 커밋 전 반드시
실행합니다.

## 제보

취약점을 발견하면 dostevskii@gmail.com 으로 알려주세요.
